/**
 * 자바 주석과 동일
 * 자바는 객체지향형이라서 무조건 클래스가 있어야 메서드를 쓸 수 있다.
 * 자바스크립트는 함수형 언어라 클래스 없이 함수를 정의해서 쓸 수 있다.
 * 함수 == 메서드
 * => 기능을 처리하는 단위
 */

function call(){
	//자바의 메서드 역할, 함수(function)
	alert('외부 파일에서 불러옴')
}